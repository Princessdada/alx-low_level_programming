#include "main.h"
/**
* _islower - a function that checks for lower case character
* @c: single letter input
* Return: ways 0 (Success)
*/
int_islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
